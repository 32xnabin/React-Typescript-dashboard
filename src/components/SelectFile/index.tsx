import { Box } from '@material-ui/core';
import { experimentalStyled } from '@material-ui/core/styles';
import SelectFile from './selectfile.png';

function Logo() {
  return <img src={SelectFile} alt="SelectFile" />;
}

export default Logo;
